{
  "name": "opa-recipe",
  "version": "1.0.1",
  "private": true,
  "type": "module",
  "description": "nominee policy rule whose `when` calls a mocked OPA-shaped decision point; the decision's reason lands on the receipt unchanged. Swap in a real OPA server with one fetch call.",
  "scripts": {
    "demo": "tsx run.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "nominee": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.22.4",
    "typescript": "^5.6.3",
    "vitest": "^4.1.10"
  }
}
