{
  "name": "fga-recipe",
  "version": "1.0.3",
  "private": true,
  "type": "module",
  "description": "nominee policy rule whose `when` calls a mocked OpenFGA/WorkOS-FGA-shaped relationship check; the decision's reason lands on the receipt unchanged. Swap in a real FGA store with one call.",
  "scripts": {
    "demo": "tsx run.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "nominee": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.22.4",
    "typescript": "^5.6.3",
    "vitest": "^4.1.10"
  }
}
