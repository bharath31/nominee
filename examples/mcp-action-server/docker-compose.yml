version: '3.8'
services:
  db:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: nominee
      POSTGRES_PASSWORD: password
      POSTGRES_DB: nominee_lifecycle
    ports:
      - "5432:5432"
