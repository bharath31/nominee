# Communities — draft, do not post

Read each community’s current rules the day you post. Self-promotion
norms differ. If a rule says no showcases, skip that community rather
than “adapt the post.”

## r/programming

Only if Show HN is already up and the gates are met. Link the HN thread,
not a raw landing page, unless their rules prefer the repo. Lead with the
injection demo being containment, not detection.

## r/LocalLLaMA

This audience is unusually good at detecting overclaim. Volunteer the
in-process wrapping limit in the first paragraph. nominee is TypeScript /
MCP / cloud-agent shaped; do not pretend it is a local-LLM guardrail
product. If the post would only be “another JS library,” skip.

## TypeScript / AI SDK Discords and forums

Prefer answering an existing HITL / tool-approval thread over a launch
blast. Link `docs/integrations/ai-sdk.md`. Say `needsApproval` is the
right default when it covers the boundary.

## Draft (only after gates)

```
Observe-mode wrap of existing tools, then allow/ask/deny before the side
effect. Does not detect prompt injection. In-process wrapping is not
isolation. No named customers yet.

https://github.com/bharath31/nominee
```
